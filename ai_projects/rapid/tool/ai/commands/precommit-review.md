# C++ Precommit Review

Load the `rapid-cpp-precommit-review` skill using the skill tool and perform the review according to its instructions.
If the skill cannot be loaded, report the blocker instead of performing a generic review.

Follow the skill's staged-first selection, snapshot isolation, read-only restrictions, and reporting format.
This command requests a review, not fixes or a commit.

Optional review focus (does not change the skill's source selection or authorize modifications):

$ARGUMENTS
